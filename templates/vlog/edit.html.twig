{% extends 'base.html.twig' %}

{% block title %}Edit Video - {{ video.name }}{% endblock %}

{% block body %}
    <h1>Edit Video</h1>

    {{ form_start(form) }}
    {{ form_row(form.file) }}
    <button class="btn">Update Video</button>
    {{ form_end(form) }}

    <a href="{{ path('vlog_dashboard') }}">Back to Dashboard</a>
{% endblock %}