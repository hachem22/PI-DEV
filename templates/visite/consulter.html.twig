{% extends 'medecin/baseMedecin.html.twig' %}

{% block title %}Consultation de la Visite - {{ visite.id }}{% endblock %}

{% block content %}
    <div class="container-xxl flex-grow-1 container-p-y">
        <div class="card" style="padding: 20px;">
            <div class="container">
                <div class="card shadow-lg p-4">
                    <h1 class="mb-4 text-center text-primary">Détails de la Visite - {{ visite.id }}</h1>

                    <ul class="list-group">
                        <li class="list-group-item"><strong>Date :</strong> {{ visite.dateVisite|date('d/m/Y') }}</li>
                        <li class="list-group-item"><strong>Diagnostic :</strong> {{ visite.diagnostic }}</li>
                        <li class="list-group-item"><strong>Ordonnance :</strong> {{ visite.ordonnance }}</li>
                        <li class="list-group-item"><strong>Certificat :</strong> {{ visite.certificat }}</li>
                    </ul>

                    <div class="mt-4">
                        <a href="{{ path('visite_index', {'id': visite.dossierMedical.id}) }}" class="btn btn-outline-primary">
                            <i class="bx bx-arrow-back"></i> Retour
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}