<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dossier Médical - {{ dossier.id }}</title>
    <style>
        /* Reset CSS */
        body, h1, h2, h3, p {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f4f7f6;
            color: #333;
            padding: 40px;
            max-width: 800px;
            margin: 0 auto;
        }

        /* Header */
        header {
            text-align: center;
            margin-bottom: 40px;
        }

        header h1 {
            font-size: 28px;
            color: #007bff;
            margin-bottom: 10px;
        }

        header p {
            font-size: 14px;
            color: #666;
        }

        .logo {
            width: 100px; /* Ajustez la taille du logo selon vos besoins */
            margin-bottom: 20px;
        }

        /* Sections */
        .section {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 20px;
        }

        .section h3 {
            font-size: 20px;
            color: #007bff;
            margin-bottom: 15px;
            border-bottom: 2px solid #007bff;
            padding-bottom: 5px;
        }

        .section p {
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 10px;
        }

        .section strong {
            color: #333;
            font-weight: 600;
        }

        /* Footer */
        footer {
            text-align: center;
            margin-top: 40px;
            font-size: 12px;
            color: #666;
        }

        /* Tableau pour les informations */
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        .info-table td {
            padding: 8px;
            border-bottom: 1px solid #eee;
        }

        .info-table td:first-child {
            font-weight: 600;
            color: #555;
            width: 30%;
        }

        .info-table td:last-child {
            color: #333;
        }

        /* Bouton fictif pour l'impression */
        .print-button {
            display: inline-block;
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 14px;
            margin-top: 20px;
            text-align: center;
        }

        .print-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <header>
        <!-- Logo -->
        <img src="{{ asset('log.png') }}" alt="Logo" class="logo">
        <h1>Dossier Médical - {{ dossier.id }}</h1>
        <p>Généré le {{ "now"|date("d/m/Y") }}</p>
    </header>

    <main>
        <!-- Section Informations sur le patient -->
        <section class="section">
            <h3>Informations sur le patient</h3>
            <table class="info-table">
                <tr>
                    <td>Nom du patient :</td>
                    <td>{{ dossier.patient.nom }}</td>
                </tr>
                <tr>
                    <td>Prénom du patient :</td>
                    <td>{{ dossier.patient.prenom }}</td>
                </tr>
            </table>
        </section>

        <!-- Section Informations générales -->
        <section class="section">
            <h3>Informations générales</h3>
            <table class="info-table">
                <tr>
                    <td>Groupe sanguin :</td>
                    <td>{{ dossier.GroupeSanguin }}</td>
                </tr>
                <tr>
                    <td>Allergies :</td>
                    <td>{{ dossier.Allergies }}</td>
                </tr>
                <tr>
                    <td>Vaccinations :</td>
                    <td>{{ dossier.Vaccinations }}</td>
                </tr>
                <tr>
                    <td>Antécédents médicaux :</td>
                    <td>{{ dossier.AntecedentsMedicaux }}</td>
                </tr>
                <tr>
                    <td>Traitements en cours :</td>
                    <td>{{ dossier.TraitementsEnCours }}</td>
                </tr>
                <tr>
                    <td>Remarques :</td>
                    <td>{{ dossier.Remarques }}</td>
                </tr>
            </table>
        </section>
    </main>

    <footer>
        <p>Ce dossier a été généré automatiquement. Pour toute question, contactez votre médecin.</p>
    </footer>
</body>
</html>