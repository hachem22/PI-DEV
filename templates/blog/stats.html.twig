<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Blog Statistics{% endblock %}</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f5f7fa;
        }

        h1 {
            color: #2c3e50;
            text-align: center;
            margin-bottom: 40px;
            font-size: 2.5rem;
        }

        h2 {
            color: #3498db;
            border-bottom: 2px solid #e7e7e7;
            padding-bottom: 10px;
            margin-top: 30px;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }

        .stat-card {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 20px;
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-card h2 {
            margin-top: 0;
            font-size: 1.5rem;
        }

        .total-posts {
            text-align: center;
            font-size: 4rem;
            font-weight: bold;
            color: #3498db;
            margin: 20px 0;
        }

        ul {
            padding-left: 0;
            list-style-type: none;
        }

        li {
            padding: 10px 0;
            border-bottom: 1px solid #f1f1f1;
            display: flex;
            justify-content: space-between;
        }

        li:last-child {
            border-bottom: none;
        }

        .count-badge {
            background-color: #3498db;
            color: white;
            border-radius: 20px;
            padding: 2px 10px;
            font-size: 0.9rem;
        }

        .monthly-chart {
            height: 300px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
<h1>Blog Statistics Dashboard</h1>

<div class="stats-container">
    <div class="stat-card">
        <h2>Total Posts</h2>
        <div class="total-posts">{{ totalPosts }}</div>
    </div>

    <div class="stat-card">
        <h2>Posts by Category</h2>
        <ul>
            {% for category in postsByCategory %}
                <li>
                    <span>{{ category.category|default('Uncategorized') }}</span>
                    <span class="count-badge">{{ category.postCount }} posts</span>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="stat-card">
        <h2>Posts by Author</h2>
        <ul>
            {% for author in postsByAuthor %}
                <li>
                    <span>{{ author.authorName|default('Unknown') }}</span>
                    <span class="count-badge">{{ author.postCount }} posts</span>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="stat-card">
        <h2>Monthly Growth</h2>
        <ul>
            {% for month in monthlyGrowth %}
                <li>
                    <span>{{ month.month }}</span>
                    <span class="count-badge">{{ month.postCount }} posts</span>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
</body>
</html>