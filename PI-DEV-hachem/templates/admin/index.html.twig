{% extends 'admin/base_admin.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h3 class="text-center mb-4">📅 Liste des événements</h3>
        
        <div class="text-right mb-3">
            <a href="{{ path('evenement_new') }}" class="btn btn-primary shadow-sm">
                <i class="fas fa-plus-circle"></i> Créer un événement
            </a>
        </div>

        <div class="table-responsive">
            <table class="table table-hover table-striped shadow-sm">
                <thead class="table-dark">
                    <tr>
                        <th>Nom</th>
                        <th>Description</th>
                        <th>Capacité</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for evenement in evenements %}
                        <tr>
                            <td>{{ evenement.Nom }}</td>
                            <td>{{ evenement.Description }}</td>
                            <td>{{ evenement.Capacite }}</td>
                            <td class="text-center">
                                <a href="{{ path('evenement_show', {'id': evenement.id}) }}" class="btn btn-info btn-sm">
                                    <i class="fas fa-eye"></i> Voir
                                </a>
                                <a href="{{ path('evenement_edit', {'id': evenement.id}) }}" class="btn btn-warning btn-sm">
                                    <i class="fas fa-edit"></i> Modifier
                                </a>
                                <form method="post" action="{{ path('evenement_delete', {'id': evenement.id}) }}" style="display:inline;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.id) }}">
                                    <button class="btn btn-danger btn-sm" type="submit" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet événement ?');">
                                        <i class="fas fa-trash-alt"></i> Supprimer
                                    </button>
                                </form>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted">Aucun événement trouvé.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
